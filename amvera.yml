meta:
  environment: jvm
  toolchain:
    name: maven
    version: 17
build:
  artifacts:
    target/*.jar: /
  args: 'spring-boot:repackage '
run:
  jarName: out/artifacts/setkaBot_jar/setkaBot.jar
  persistenceMount: /data
  containerPort: 80
